import 'package:equatable/equatable.dart';

class {{extra.name}}State extends Equatable {
  {{extra.name}}State._();

  factory {{extra.name}}State.initial() {
    return {{extra.name}}State._();
  }

  {{extra.name}}State copyWith() {
    return {{extra.name}}State._();
  }

  @override
  List<Object> get props => [];
}
